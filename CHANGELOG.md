# 更新公告

## v2.3.8 (2030080)

### 新增功能
- **图片加速（官方 2.3.6 ECH）**：设置 → 网络设置 → 图片加速，支持 直连 / ECH / 镜像 三模式（默认 ECH），Bangumi 封面与头像加载更快更稳定
- **网络镜像设置升级**：番剧条目镜像、规则仓库镜像、图片加速 统一到「访问加速」区
- **扫码登录修复**：已登录设备扫码 → 未登录设备直接登录成功（后端 confirm.php 支持扫码者 token 直接确认，去掉确认框流程）
- **追番页白屏防护**：收藏 type 脏数据（0/越界）不再让追番页崩溃白屏——源头钳制 + 读取拦截 + 渲染兜底三层防御
- **偏好设置按使用次数排序**：我的页「偏好设置」（外观/播放/弹幕）点击使用次数 +1，用得越多排越前，按钮带次数角标
- **形变加载动画**：下载解析中、评分提交、图片预览、存储错误页改用 MaterialShapes 形变动画（播放页保持原样式）
- **视频分享播放页升级**：video.php 播放页新增 p2p_mp4 新版统计面板（HTTP/P2P 下载速度、P2P 上传、真实在线人数、数据块来源、堆叠进度条），自建 p2p_stats.php 统计后端（60 秒心跳聚合真实在线人数）
- **观看热力图 + 收藏批量管理**
- **下载失败自动重试 + 账号快速切换**（均持久化存储）
- **更新弹窗「忽略该版本」**
- **追番页下载速度显示**
- **登录二维码中心加 LOGO + 高纠错**（扫码成功率提升）
- **App Links 网页深链支持**（浏览器打开链接直达详情页）
- **静默下载更新默认开启，更新渠道默认预览版**
- 官方 2.3.2+2.3.3 基础合并：设置页宽屏分栏、低内存模式网络感知、评分可见性、退出播放页加载指示器

### 修复
- 同步后收藏变灰/白屏（收藏箱损坏自动抢救、恢复追番页逻辑）
- 扫码登录协议不匹配（yhdmgz:// 兼容 yhdm://）
- WebDAV 收藏同步 DateTime 崩溃
- 网络类播放错误静默处理，不再弹错
- 预加载被主解析抢占后自动补一次（不再反复进冷却）
- 搜索输入框布局变化时保持连接
- 打开番剧详情页路由修复
- 移除弹幕屏蔽词（关键词）云端同步（可能引起同步白屏；本地屏蔽功能保留）

### 构建
- 适配 Flutter 3.47 / flutter_modular v7
- 包名 com.predidit.YHDM

---

## v2.3.5 (2030050)

### 新增功能
- **Bangumi 登录 UI 重做**：模仿 Animeko 简洁风格，点击按钮跳转浏览器授权，无需手动粘贴 Token
- **HLS 伪装流嗅探**：自动识别 `.webp`/`.png` 等伪装后缀的 HLS 流（如次元城动画），强制 HLS 解复用播放
- **网页版分享链接深链适配**：支持 `qlyyz.xyz/yhdm/detail.html?id=xxx` 格式，自动跳转详情页
- **弹幕集数修复**：解决列表倒序时弹幕指向错误集数的问题
- **Android 后台暂停预取**（官方 2.2.9）：防止后台切换返回后播放卡死
- **Material 3 UI 改进**（官方 2.2.9）：源选择列表、设置页面视觉优化
- **一起看面板**（官方 2.2.9）：Syncplay 功能入口

### 合并官方 2.2.8 → 2.2.9 更新
- Android 后台暂停预取（防止后台播放卡死）
- Material 3 outlined text fields
- PIP rect 缓存改进
- 源选择 UI 重设计
- 日志精简（去除 verbose stacktrace）
- Flutter 3.47.0 → 3.47.1

### 修复
- HLS 嗅探 Range 请求不稳定（改用流式读取前 1KB）
- 弹幕集数 fallback 使用 1-based（listIndex+1）
- 搜索失败日志过于冗长（去除 stacktrace 输出）
- 登录页面路由路径修正

### 构建
- 包名改为 `com.predidit.YHDM`
- 版本号动态同步（构建时自动更新 api_endpoints.dart + pubspec.yaml）
- 新 Logo 图片
- 新增 `flutter_launcher_icons` 构建步骤（自动重生成图标）

---

## v2.3.4 (2030040)
- 初始版本
